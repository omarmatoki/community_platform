# دليل الإعداد الشامل - منصة تعزيز الوعي المجتمعي

هذا دليل شامل لإعداد وتشغيل المشروع من الصفر.

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Node.js** (الإصدار 14 أو أحدث)
   - تحميل من: https://nodejs.org/
   - تحقق من التثبيت: `node --version`

2. **MySQL** (الإصدار 5.7 أو أحدث)
   - تحميل من: https://dev.mysql.com/downloads/mysql/
   - أو استخدم XAMPP/WAMP
   - تحقق من التثبيت: `mysql --version`

3. **Git** (اختياري)
   - تحميل من: https://git-scm.com/

4. **محرر نصوص** (VS Code مُوصى به)
   - تحميل من: https://code.visualstudio.com/

## خطوات الإعداد

### 1. إعداد قاعدة البيانات MySQL

#### الطريقة 1: عبر سطر الأوامر

```bash
# تسجيل الدخول إلى MySQL
mysql -u root -p

# إنشاء قاعدة البيانات
CREATE DATABASE community_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# التحقق من إنشاء القاعدة
SHOW DATABASES;

# الخروج
EXIT;
```

#### الطريقة 2: عبر phpMyAdmin (XAMPP/WAMP)

1. افتح phpMyAdmin: `http://localhost/phpmyadmin`
2. اضغط على "New" في القائمة اليسرى
3. اسم قاعدة البيانات: `community_platform`
4. Collation: `utf8mb4_unicode_ci`
5. اضغط "Create"

### 2. تحميل المشروع

إذا كنت تستخدم Git:

```bash
git clone <repository-url>
cd backend
```

أو قم بفك ضغط المشروع والانتقال إلى مجلد backend.

### 3. تثبيت الحزم

```bash
npm install
```

هذا سيثبت جميع المكتبات المطلوبة:
- express
- sequelize
- mysql2
- bcryptjs
- jsonwebtoken
- dotenv
- cors
- express-validator

### 4. إعداد متغيرات البيئة

افتح ملف `.env` وقم بتعديل البيانات:

```env
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=community_platform
DB_DIALECT=mysql
JWT_SECRET=your_very_long_and_secret_key_here_min_32_chars
JWT_EXPIRE=7d
NODE_ENV=development
```

**هام:**
- استبدل `your_mysql_password` بكلمة مرور MySQL الخاصة بك
- غيّر `JWT_SECRET` إلى سلسلة عشوائية طويلة

### 5. اختبار الاتصال بقاعدة البيانات

قبل تشغيل السيرفر، يمكنك اختبار الاتصال:

```bash
node -e "require('./config/database').testConnection()"
```

يجب أن ترى: `✓ تم الاتصال بقاعدة البيانات بنجاح`

### 6. تشغيل السيرفر

#### وضع التطوير (Development):

```bash
npm run dev
```

السيرفر سيعمل على `http://localhost:5000` مع إعادة التشغيل التلقائي عند التعديل.

#### وضع الإنتاج (Production):

```bash
npm start
```

### 7. إنشاء مستخدم Admin

بعد تشغيل السيرفر بنجاح، افتح terminal جديد:

```bash
npm run create-admin
```

سترى:

```
✅ تم إنشاء مستخدم Admin بنجاح!
═══════════════════════════════════════════
📧 البريد الإلكتروني: admin@community.com
🔑 كلمة المرور: Admin@123456
👤 الاسم: المدير العام
🎯 الدور: admin
═══════════════════════════════════════════
```

**احفظ هذه البيانات!**

### 8. اختبار API

#### الطريقة 1: عبر المتصفح

افتح: `http://localhost:5000`

يجب أن ترى رسالة ترحيب JSON.

#### الطريقة 2: عبر cURL

```bash
# اختبار المسار الرئيسي
curl http://localhost:5000

# تسجيل دخول Admin
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@community.com","password":"Admin@123456"}'
```

#### الطريقة 3: عبر Postman

1. افتح Postman
2. أنشئ طلب جديد
3. Method: `POST`
4. URL: `http://localhost:5000/api/auth/login`
5. Body → raw → JSON:
```json
{
  "email": "admin@community.com",
  "password": "Admin@123456"
}
```
6. اضغط Send

احفظ الـ `token` من الرد!

### 9. إنشاء بيانات تجريبية

#### إنشاء تصنيف:

```bash
POST http://localhost:5000/api/categories
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json

{
  "name": "الصحة",
  "description": "مقالات عن الصحة والوعي الصحي"
}
```

#### إنشاء مقال:

```bash
POST http://localhost:5000/api/articles
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json

{
  "title": "أهمية النظافة الشخصية",
  "content": "محتوى المقال...",
  "categoryId": "CATEGORY_ID_FROM_PREVIOUS_STEP"
}
```

## هيكل قاعدة البيانات

بعد تشغيل السيرفر لأول مرة، سيتم إنشاء الجداول التالية تلقائياً:

- `users` - المستخدمون
- `categories` - التصنيفات
- `articles` - المقالات
- `article_reads` - سجل قراءة المقالات
- `surveys` - الاستبيانات
- `questions` - الأسئلة
- `options` - خيارات الإجابة
- `user_answers` - إجابات المستخدمين
- `games` - الألعاب
- `user_games` - سجل ألعاب المستخدمين
- `polls` - استطلاعات الرأي
- `poll_votes` - أصوات الاستطلاعات
- `discussion_sessions` - الجلسات الحوارية
- `session_attendance` - حضور الجلسات

## التحقق من نجاح الإعداد

### Checklist:

- [ ] MySQL يعمل بشكل صحيح
- [ ] قاعدة البيانات `community_platform` موجودة
- [ ] تم تثبيت جميع الحزم (`node_modules` موجود)
- [ ] ملف `.env` تم إعداده بشكل صحيح
- [ ] السيرفر يعمل على المنفذ 5000
- [ ] الجداول تم إنشاؤها في قاعدة البيانات
- [ ] مستخدم Admin تم إنشاؤه
- [ ] يمكن تسجيل الدخول كـ Admin
- [ ] يمكن إنشاء تصنيف (اختبار صلاحيات Admin)

## المشاكل الشائعة وحلولها

### المشكلة 1: خطأ في الاتصال بقاعدة البيانات

```
Error: SequelizeConnectionError: Access denied for user 'root'@'localhost'
```

**الحل:**
- تأكد من أن MySQL يعمل
- تحقق من اسم المستخدم وكلمة المرور في `.env`
- تأكد من إنشاء قاعدة البيانات

### المشكلة 2: المنفذ 5000 مشغول

```
Error: listen EADDRINUSE: address already in use :::5000
```

**الحل:**
- غيّر المنفذ في `.env` إلى 3000 أو أي منفذ آخر
- أو أوقف العملية التي تستخدم المنفذ 5000

**Windows:**
```bash
netstat -ano | findstr :5000
taskkill /PID <PID> /F
```

**Linux/Mac:**
```bash
lsof -ti:5000 | xargs kill -9
```

### المشكلة 3: الجداول لم يتم إنشاؤها

**الحل:**
- تأكد من تشغيل السيرفر مرة واحدة على الأقل
- في ملف `server.js`، تأكد من وجود `sequelize.sync()`
- يمكنك استخدام `sequelize.sync({ force: true })` لإعادة إنشاء الجداول (⚠️ سيحذف البيانات)

### المشكلة 4: خطأ في تشفير الأحرف العربية

**الحل:**
- تأكد من أن قاعدة البيانات تستخدم `utf8mb4_unicode_ci`
- تأكد من إعدادات Sequelize في `config/database.js`

```sql
ALTER DATABASE community_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### المشكلة 5: JWT Secret قصير جداً

**الحل:**
- استخدم سلسلة طويلة عشوائية (32 حرف على الأقل)
- يمكنك توليد واحدة: https://randomkeygen.com/

### المشكلة 6: Cannot find module

```
Error: Cannot find module 'express'
```

**الحل:**
```bash
npm install
```

## الخطوات التالية

بعد إكمال الإعداد بنجاح:

1. ✅ غيّر كلمة مرور Admin
2. ✅ أنشئ بيانات تجريبية (تصنيفات، مقالات، ألعاب)
3. ✅ اختبر جميع endpoints
4. ✅ راجع ملف `API_DOCUMENTATION.md`
5. ✅ راجع ملف `POSTMAN_EXAMPLES.md`

## موارد إضافية

- **التوثيق الكامل للـ API**: `API_DOCUMENTATION.md`
- **أمثلة Postman**: `POSTMAN_EXAMPLES.md`
- **إنشاء Admin**: `CREATE_ADMIN.md`
- **README الرئيسي**: `README.md`

## الدعم

إذا واجهت أي مشاكل:

1. تحقق من ملف `SETUP_GUIDE.md` (هذا الملف)
2. راجع المشاكل الشائعة أعلاه
3. تحقق من console للأخطاء
4. تأكد من أن جميع المتطلبات مثبتة

## ملاحظات الإنتاج

عند نشر المشروع للإنتاج:

1. غيّر `NODE_ENV` إلى `production`
2. استخدم JWT_SECRET قوي وعشوائي
3. فعّل HTTPS
4. استخدم password قوي لقاعدة البيانات
5. راجع إعدادات CORS
6. فعّل rate limiting
7. استخدم PM2 لإدارة العملية
8. احتفظ بنسخ احتياطية من قاعدة البيانات

---

**مبروك! المشروع جاهز للعمل! 🎉**
