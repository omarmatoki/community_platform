# ✅ قائمة التحقق - منصة صوتنا يبني

استخدم هذه القائمة للتأكد من إعداد المشروع بشكل صحيح واختبار جميع الميزات.

---

## 📋 الإعداد الأولي

### المتطلبات الأساسية

- [ ] Node.js مثبت (تحقق: `node --version`)
- [ ] npm مثبت (تحقق: `npm --version`)
- [ ] MySQL مثبت ويعمل (تحقق: `mysql --version`)
- [ ] محرر نصوص جاهز (VS Code مُوصى به)

### إعداد قاعدة البيانات

- [ ] MySQL Server يعمل
- [ ] قاعدة بيانات `community_platform` تم إنشاؤها
- [ ] Character set: `utf8mb4_unicode_ci`
- [ ] يمكن الاتصال بقاعدة البيانات

### إعداد المشروع

- [ ] مجلد `backend` موجود
- [ ] تم تشغيل `npm install` بنجاح
- [ ] مجلد `node_modules` موجود
- [ ] ملف `.env` تم تعديله بشكل صحيح
- [ ] `DB_PASSWORD` تم تعيينها
- [ ] `JWT_SECRET` تم تغييرها

---

## 🚀 التشغيل

### تشغيل السيرفر

- [ ] تم تشغيل `npm run dev` بنجاح
- [ ] رسالة "✓ تم الاتصال بقاعدة البيانات بنجاح" ظهرت
- [ ] رسالة "✓ تمت مزامنة قاعدة البيانات بنجاح" ظهرت
- [ ] رسالة "🚀 السيرفر يعمل على المنفذ 5000" ظهرت
- [ ] لا توجد أخطاء في console

### التحقق من الجداول

- [ ] افتح MySQL وتحقق من الجداول:
  - [ ] `users`
  - [ ] `categories`
  - [ ] `articles`
  - [ ] `article_reads`
  - [ ] `surveys`
  - [ ] `questions`
  - [ ] `options`
  - [ ] `user_answers`
  - [ ] `games`
  - [ ] `user_games`
  - [ ] `polls`
  - [ ] `poll_votes`
  - [ ] `discussion_sessions`
  - [ ] `session_attendance`

### إنشاء Admin

- [ ] تشغيل `npm run create-admin` في terminal جديد
- [ ] رسالة "✅ تم إنشاء مستخدم Admin بنجاح!" ظهرت
- [ ] البريد الإلكتروني: `admin@community.com`
- [ ] كلمة المرور: `Admin@123456`
- [ ] تم حفظ البيانات

---

## 🧪 اختبار API

### اختبار المسار الرئيسي

- [ ] فتح `http://localhost:5000` في المتصفح
- [ ] ظهور رسالة JSON ترحيبية
- [ ] قائمة endpoints موجودة

### اختبار المصادقة

#### تسجيل مستخدم جديد
```http
POST http://localhost:5000/api/auth/register
```
- [ ] يعمل بشكل صحيح
- [ ] يعيد token
- [ ] المستخدم موجود في قاعدة البيانات

#### تسجيل دخول Admin
```http
POST http://localhost:5000/api/auth/login
```
- [ ] يعمل مع بيانات Admin
- [ ] يعيد token
- [ ] role = admin

#### عرض الملف الشخصي
```http
GET http://localhost:5000/api/auth/profile
Authorization: Bearer TOKEN
```
- [ ] يعيد بيانات المستخدم
- [ ] كلمة المرور مخفية

### اختبار صلاحيات Admin

#### إنشاء تصنيف
```http
POST http://localhost:5000/api/categories
Authorization: Bearer ADMIN_TOKEN
```
- [ ] Admin يستطيع الإنشاء
- [ ] User عادي لا يستطيع (403)
- [ ] بدون token لا يستطيع (401)

#### إنشاء مقال
```http
POST http://localhost:5000/api/articles
Authorization: Bearer ADMIN_TOKEN
```
- [ ] يعمل مع Admin token
- [ ] المقال موجود في قاعدة البيانات
- [ ] adminId صحيح

### اختبار نظام النقاط

#### قراءة مقال (5 نقاط)
```http
POST http://localhost:5000/api/articles/:id/read
Authorization: Bearer USER_TOKEN
```
- [ ] نقاط المستخدم زادت بـ 5
- [ ] سجل في جدول `article_reads`
- [ ] محاولة ثانية ترفض

#### إكمال استبيان (10 نقاط)
```http
POST http://localhost:5000/api/surveys/:id/submit
Authorization: Bearer USER_TOKEN
```
- [ ] يحسب النتيجة بشكل صحيح
- [ ] يمنح 10 نقاط عند 70%+
- [ ] لا يمنح نقاط عند < 70%
- [ ] محاولة ثانية ترفض

#### إكمال لعبة (15 نقطة)
```http
POST http://localhost:5000/api/games/:id/complete
Authorization: Bearer USER_TOKEN
```
- [ ] نقاط المستخدم زادت بـ 15
- [ ] سجل في جدول `user_games`
- [ ] الرسالة التعليمية ظهرت
- [ ] محاولة ثانية ترفض

#### التصويت (5 نقاط)
```http
POST http://localhost:5000/api/polls/:id/vote
Authorization: Bearer USER_TOKEN
```
- [ ] نقاط المستخدم زادت بـ 5
- [ ] الصوت مسجل
- [ ] محاولة ثانية ترفض

#### حضور جلسة (20 نقطة)
```http
POST http://localhost:5000/api/discussions/:id/attend
Authorization: Bearer USER_TOKEN
```
- [ ] نقاط المستخدم زادت بـ 20
- [ ] الحضور مسجل
- [ ] محاولة ثانية ترفض

### اختبار لوحة الصدارة

```http
GET http://localhost:5000/api/users/leaderboard
```
- [ ] تعرض المستخدمين حسب النقاط
- [ ] الترتيب صحيح
- [ ] البيانات كاملة

---

## 🔐 اختبار الأمان

### JWT Token

- [ ] طلب بدون token يرفض (401)
- [ ] طلب بـ token خاطئ يرفض (401)
- [ ] طلب بـ token منتهي يرفض (401)
- [ ] طلب بـ token صحيح يعمل

### الصلاحيات

- [ ] User عادي لا يستطيع إنشاء مقال (403)
- [ ] User عادي لا يستطيع حذف تصنيف (403)
- [ ] Admin يستطيع جميع العمليات
- [ ] User يستطيع القراءة فقط

### تشفير كلمة المرور

- [ ] كلمة المرور مشفرة في قاعدة البيانات
- [ ] كلمة المرور غير مرئية في الردود
- [ ] تسجيل الدخول بكلمة مرور خاطئة يرفض
- [ ] تغيير كلمة المرور يعمل

### منع التكرار

- [ ] لا يمكن قراءة نفس المقال مرتين للنقاط
- [ ] لا يمكن إرسال نفس الاستبيان مرتين
- [ ] لا يمكن إكمال نفس اللعبة مرتين للنقاط
- [ ] لا يمكن التصويت في نفس الاستطلاع مرتين
- [ ] لا يمكن تسجيل حضور نفس الجلسة مرتين للنقاط

---

## 📊 اختبار CRUD

### التصنيفات
- [ ] إنشاء (POST) ✓
- [ ] عرض الكل (GET) ✓
- [ ] عرض واحد (GET) ✓
- [ ] تحديث (PUT) ✓
- [ ] حذف (DELETE) ✓

### المقالات
- [ ] إنشاء (POST) ✓
- [ ] عرض الكل (GET) ✓
- [ ] عرض واحد (GET) ✓
- [ ] عرض حسب التصنيف (GET) ✓
- [ ] تحديث (PUT) ✓
- [ ] حذف (DELETE) ✓

### الاستبيانات
- [ ] إنشاء (POST) ✓
- [ ] عرض حسب المقال (GET) ✓
- [ ] إرسال إجابات (POST) ✓
- [ ] عرض النتائج (GET) ✓

### الألعاب
- [ ] إنشاء (POST) ✓
- [ ] عرض الكل (GET) ✓
- [ ] عرض واحدة (GET) ✓
- [ ] تحديث (PUT) ✓
- [ ] حذف (DELETE) ✓
- [ ] إكمال (POST) ✓

### استطلاعات الرأي
- [ ] إنشاء (POST) ✓
- [ ] عرض الكل (GET) ✓
- [ ] عرض واحد (GET) ✓
- [ ] تحديث (PUT) ✓
- [ ] حذف (DELETE) ✓
- [ ] التصويت (POST) ✓
- [ ] عرض النتائج (GET) ✓

### الجلسات الحوارية
- [ ] إنشاء (POST) ✓
- [ ] عرض الكل (GET) ✓
- [ ] عرض القادمة (GET?upcoming=true) ✓
- [ ] عرض واحدة (GET) ✓
- [ ] تحديث (PUT) ✓
- [ ] حذف (DELETE) ✓
- [ ] تسجيل حضور (POST) ✓
- [ ] عرض الحضور (GET) ✓

---

## 🌐 اختبار السيناريوهات الكاملة

### سيناريو 1: User Journey

1. - [ ] تسجيل مستخدم جديد
2. - [ ] تسجيل الدخول
3. - [ ] عرض التصنيفات
4. - [ ] عرض المقالات
5. - [ ] قراءة مقال → 5 نقاط ✓
6. - [ ] حل استبيان المقال → 10 نقاط ✓
7. - [ ] لعب لعبة → 15 نقطة ✓
8. - [ ] التصويت في استطلاع → 5 نقاط ✓
9. - [ ] حضور جلسة → 20 نقطة ✓
10. - [ ] **المجموع: 55 نقطة ✓**
11. - [ ] عرض ترتيب في لوحة الصدارة

### سيناريو 2: Admin Journey

1. - [ ] تسجيل دخول كـ Admin
2. - [ ] إنشاء 3 تصنيفات
3. - [ ] إنشاء 5 مقالات
4. - [ ] إنشاء استبيان لكل مقال
5. - [ ] إنشاء 3 ألعاب
6. - [ ] إنشاء 2 استطلاعات رأي
7. - [ ] جدولة 2 جلسات حوارية
8. - [ ] عرض جميع المستخدمين
9. - [ ] تحديث نقاط مستخدم
10. - [ ] عرض حضور جلسة

---

## 📝 التوثيق

### ملفات التوثيق

- [ ] `README.md` موجود ومقروء
- [ ] `API_DOCUMENTATION.md` موجود وشامل
- [ ] `POSTMAN_EXAMPLES.md` موجود وواضح
- [ ] `CREATE_ADMIN.md` موجود ومفيد
- [ ] `SETUP_GUIDE.md` موجود ومفصل
- [ ] `PROJECT_SUMMARY.md` موجود وشامل
- [ ] `START_HERE.md` موجود وموجه
- [ ] `CHECKLIST.md` موجود (هذا الملف!)

### الكود

- [ ] جميع الملفات تحتوي تعليقات عربية
- [ ] الكود منظم ونظيف
- [ ] لا توجد أخطاء في console
- [ ] الرسائل واضحة ومفيدة

---

## 🎯 الإنجاز النهائي

### عند الانتهاء من جميع الخطوات:

- [ ] السيرفر يعمل بشكل مستقر
- [ ] جميع Endpoints تعمل
- [ ] نظام النقاط يعمل بشكل صحيح
- [ ] منع التكرار يعمل
- [ ] الأمان محكم
- [ ] التوثيق كامل
- [ ] جاهز للتطوير!

---

## 🏆 النتيجة النهائية

**إذا كانت جميع العلامات ✓**:

### 🎉 مبروك! المشروع جاهز بنسبة 100%

يمكنك الآن:
1. ✅ بدء تطوير Frontend
2. ✅ إضافة ميزات جديدة
3. ✅ نشر المشروع
4. ✅ دعوة المستخدمين

---

**ملاحظة**: احفظ هذا الملف واستخدمه كمرجع عند الحاجة!

**آخر تحديث**: تم إنشاء المشروع بالكامل ✅
